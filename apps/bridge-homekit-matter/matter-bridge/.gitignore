node_modules/
dist/
.env
.env.local
.env.*.local

# Matter fabric state — contains the bridge's private keys for the
# fabric. NEVER commit. Production: mounted as a Docker named volume
# (/data/fabric) instead of a bind mount into the repo.
.matter-fabric/

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp
.DS_Store

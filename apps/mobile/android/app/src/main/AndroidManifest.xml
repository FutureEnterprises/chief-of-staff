<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!--
    COYL Android permissions.

    Most of these are also injected by the with-coyl-android config
    plugin during `expo prebuild`, so this manifest stays in sync
    with the generated one. They are duplicated here so a manually-
    edited Android Studio open of this project (without re-prebuild)
    still has every entitlement COYL needs at runtime.
  -->

  <!-- Step / movement classification for danger-window detection. -->
  <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

  <!-- Required to fire interrupt notifications on Android 13+. -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <!-- Geofence-based danger windows. Background variant requires the
       foreground variant to also be granted. -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

  <!-- Google Fit body sensor read (legacy fallback for devices that
       don't expose Health Connect). Health Connect itself uses its
       own permission model via PermissionController. -->
  <uses-permission android:name="com.google.android.gms.permission.FITNESS_BODY_READ" />

  <!-- Other standard permissions used by the app. -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />

  <!--
    Package visibility queries (Android 11+). Without these the
    PackageManager hides Google Fit + Health Connect from us when we
    try to resolve them via Intent, which breaks the permission flow
    on a device that has them installed.
  -->
  <queries>
    <package android:name="com.google.android.apps.fitness" />
    <package android:name="com.google.android.apps.healthdata" />
  </queries>

  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:supportsRtl="true">

    <activity
      android:name=".MainActivity"
      android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:theme="@style/Theme.App.SplashScreen"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="coyl.ai" />
        <data android:scheme="https" android:host="www.coyl.ai" />
      </intent-filter>
    </activity>
  </application>
</manifest>
